import { ReactNode } from "react"

interface DocTableOptionsItemIconProps {
    children: ReactNode
}

export function DocTableOptionsItemIcon({ children }: DocTableOptionsItemIconProps) {
    return(
        <>
            { children }
        </>
    )
}